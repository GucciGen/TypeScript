import {electronics,clothes,books} from "./data"; import {findProduct,filterByPrice} from "./utils"; import {addToCart,calculateTotal,CartItem} from "./cart"; import {BaseProduct} from "./types"; type AnyProduct=BaseProduct; const phone=findProduct(electronics,1); let cart:CartItem<AnyProduct>[]=[]; cart=addToCart(cart,phone,1); const cheap=filterByPrice(electronics,15000); const total=calculateTotal(cart); console.log(cart,cheap,total);